import { Routes } from '@angular/router';
import { terminosCondicionesComponent } from './terminos-condiciones.component';

export const TERMINOS_CONDICIONES_ROUTES: Routes = [
  { path: '', component: terminosCondicionesComponent },
];
